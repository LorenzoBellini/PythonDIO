# Inteiro para float
preco = 10
preco = float(preco) # Esta linha de código converte a variável de inteiro para float
print (preco)
# Também podemos dividir um número inteiro por outro valor, isso fará com que o int se torne float